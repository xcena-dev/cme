\* Liveness WITHOUT the recovery-first schedule -- weak fairness on recovery alone. EXPECTED TO
\* FAIL (N>=3): unbounded churn starves a ready recovery, motivating liveRF.cfg.
SPECIFICATION FairSpec

CONSTANTS
    NumPeers   = 4
    NumDomains = 2

PROPERTY RecoveryTerminates
